## 选做题：设计一个简单的计算器

编写程序设计一个简单的计算器，要求支持用户输入如下格式的表达式：
操作数 1 运算符 op 操作数 2
然后，计算并输出表达式的值。支持的运算符包括：加（`+`）、减（`-`）、乘（`*`）和除（`/`）。

### 本题解决的问题：

- 支持错误处理，确保输入合法；
- 支持小数计算；
- 支持第一个操作数为负数的情况。

### 运行结果

![alt text](Calculator.png)
